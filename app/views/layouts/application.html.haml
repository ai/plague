!!! 5
%html
  %head
    %meta( charset='UTF-8' )
    %title
      - title = yield(:title)
      = title.present? ? title + ' — ' +story['title'] : story['title']

    = favicon_link_tag
    = enable_html5_for_ie
    = stylesheet_link_tag :quick
    = stylesheet_link_tag :application
    = include_fonts

    = include_jquery
    = javascript_include_tag :application

    = yield :head

    - if Rails.env.production?
      = render 'shared/counter'
  %body
    .full-loader
      .center
        Загрузка…
        .ouroboros
    = yield
