.title-page.page
  .book
    .book-wrap
      .bottom
      - (1..12).to_a.reverse_each do |i|
        .page( class="page#{i} #{(i % 2).zero? ? 'even' : 'odd'}" )
      .title
        .head
        = @title.html
      %a.open-badge.new( href="/" )
        .border.no-right
        .crop
          .arrow
            .border
        Начать чтение
