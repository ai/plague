.title-page.page
  .book
    .book-wrap
      .bottom
      .title
        - (1..9).to_a.reverse_each do |i|
          .page( class="page#{i} #{(i % 2).zero? ? 'even' : 'odd'}" )
        .head
        = @title.html
